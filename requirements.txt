# requirements.txt
# ─────────────────────────────────────────────────────────
# Core app
streamlit>=1.32.0

# HTTP clients
httpx>=0.27.0          # async requests (job_aggregator.py)
requests>=2.31.0       # sync requests (job_link_resolver.py)

# Data processing
pandas>=2.2.0

# TOML parsing (secrets — built into Python 3.11+, belt-and-braces below)
tomli>=2.0.1; python_version < "3.11"
